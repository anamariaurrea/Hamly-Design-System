// Linear.tsx
import * as React from 'react';
import { ProgressBar } from 'react-native-paper';

export const Linear = (props: React.ComponentProps<typeof ProgressBar>) => (
  <ProgressBar {...props} />
);
