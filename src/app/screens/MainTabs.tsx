import * as React from "react";
import { View, Text } from "react-native";

const MainTabs = () => (
  <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
    <Text>MainTabs Screen (placeholder)</Text>
  </View>
);

export default MainTabs;
